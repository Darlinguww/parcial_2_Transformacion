version: 2

models:
  - name: dim_customers
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null

  - name: fct_orders
    columns:
      - name: order_id
        tests:
          - unique
          - not_null
      - name: net_amount
        tests:
          - not_null

  - name: mart_customer_kpis
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null
